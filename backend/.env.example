# Copy this file to .env and fill the values locally for development.
# For Railway, set these as project environment variables / secrets instead.

# Server
PORT=3001

# Hedera credentials (testnet)
SENDER_ACCOUNT_ID=0.0.xxxxxx
SENDER_PRIVATE_KEY=302e020100300... (your ECDSA private key)
RECIPIENT_ACCOUNT_ID=0.0.xxxxxx
TOKEN_ID=0.0.xxxxxx

# AI / Gemini API key (required for the agent service)
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: set HEDERA_NETWORK to "testnet" or "mainnet" if you extend client creation
# HEDERA_NETWORK=testnet
